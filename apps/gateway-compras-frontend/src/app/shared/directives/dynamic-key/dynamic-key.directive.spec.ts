import { DynamicKeyDirective } from './dynamic-key.directive';

describe('DynamicKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicKeyDirective();
    expect(directive).toBeTruthy();
  });
});
